# AI Accelerator - Next Steps

Post-install checklist. Work through each item to complete your setup.

## 1. Review Generated Settings

The installer generated two settings files:

### .claude/settings.json (committed, shared)
Contains a baseline permissions whitelist for your detected tech stack. Review it and add project-specific patterns as needed. This file is committed to version control so your team shares the same baseline.

### .claude/settings.local.json (gitignored, secrets)
Contains placeholder `env` entries for tokens and secrets. Fill in your actual values. This file is NOT committed.

### Fix: Agent Teams Feature Flag

The installer detected that the agent teams flag is not set. Add to `~/.claude/settings.json`:
```json
{
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "preferences": {
    "teammateMode": "auto"
  }
}
```
Then restart Claude Code.

## 2. Fill in CLAUDE.md

Open `CLAUDE.md` and fill in the remaining TODO sections:
- [ ] Project Overview (2-3 sentences)
- [ ] Tech Stack table (verify auto-detected values)
- [ ] Build Commands (verify auto-detected values)
- [ ] Project Structure (key directories)
- [ ] Project-Specific Conventions (coding standards, naming)
- [ ] Roles (your application's roles and access levels)
- [ ] Docker & Database (if applicable)

Tip: You can ask Claude Code to help: "Read my project and fill in the CLAUDE.md TODOs"

## Code Review

The `/gitlab-code-review` skill supports GitLab only in v1. GitHub and Bitbucket automated code review is coming in v2.

Use `/review-local` for local code review before pushing. The `code-review-final` agent still works — but automated MR/PR commenting requires GitLab + `glab` for now.

## External Skills (Recommended)

These skills enhance the agents but are not bundled:

### /security-review (Anthropic Marketplace)
Used by the security-review-final agent for OWASP scanning.

### /frontend-design (Anthropic Marketplace)
Used by the frontend-dev agent for UI design work.

### /ui-ux-pro-max (GitHub)
Advanced UI/UX patterns. Source: https://github.com/nextlevelbuilder/ui-ux-pro-max-skill

## Quick Test

Try this in Claude Code:
```
Create a simple health check endpoint with a unit test.
Use the backend-dev agent.
```
If the agent runs, creates code, and tests pass — you're good to go.

## What's Next

- **Small changes**: Use Assisted Mode (single agent, no team orchestration)
- **Features**: Use Full Pipeline Mode (`workflow-orchestrator` agent handles everything)
- **Learn more**: Read the docs in the `docs/` directory of the AI Accelerator repo

---
*Generated by AI Accelerator v1.1.0*
