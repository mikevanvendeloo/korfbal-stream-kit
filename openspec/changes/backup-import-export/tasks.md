# Tasks: backup-import-export

> Status: COMPLETE — All tasks implemented. This is a reverse-generated spec.

## Backend

- [x] `GET /api/backup/skills/export` and `POST /api/backup/skills/import`
- [x] `GET /api/backup/positions/export` and `POST /api/backup/positions/import`
- [x] `GET /api/backup/persons/export` and `POST /api/backup/persons/import`
- [x] `GET /api/backup/matches/export` and `POST /api/backup/matches/import`
- [x] `GET /api/backup/clubs/export` and `POST /api/backup/clubs/import`
- [x] `GET /api/backup/sponsors/export` and `POST /api/backup/sponsors/import`
- [x] `GET /api/backup/settings/export` and `POST /api/backup/settings/import`
- [x] `GET /api/backup/segment-templates/export` and `POST /api/backup/segment-templates/import`
- [x] `GET /api/backup/producties/export` — full relational graph serialization
- [x] `POST /api/backup/producties/import` — full relational import with ID re-mapping

## Frontend

- [x] SettingsPage backup tab — export/import buttons for all entities
- [x] Production import UI on ProductionsAdminPage
